
#!/usr/bin/env bash
set -euo pipefail
# Requires: render.yaml at repo root and Render CLI (optional)
echo "Render will auto-detect Dockerfile. Push to your repo and create a new Web Service from this repo."
echo "Health check: /health"
